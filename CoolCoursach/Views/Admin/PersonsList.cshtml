@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<CoolCoursach.Models.User>
<h4 class="display-4">Список всех людей</h4>
<div class="row">
        <div class="btn btn-primary">@Html.ActionLink("Добавить человека", "AddPerson", "Admin",new{},
        new{@style="color:white;text-decoration: none;"})</div>
        </div>
@*<table>
    <tr>
        <td>Фото</td>
        <td>Имя</td>
        <td>Фамилия</td>
        <td>Отчество</td>
        <td>Группа</td>
        <td>Специальность</td>
        <td>Статус</td>
    </tr>
    @foreach(var User in Model){
    <tr>
        <td>
            @if(User.Photo != null)
            {
                <img style="width:80px;height:60px;" src="data:image/jpeg;base64,@(Convert.ToBase64String(User.Photo))"/>
            }
            </td>
            <td>@User.Email</td>
         <td>@User.Surname</td>
        <td>@User.Patronymic</td>
        <td>@User.GroupName</td>
        <td>@User.FacultName</td>
    
    <td>@Html.ActionLink("Редактировать пользователя","EditPerson","Admin", new{Id = User.Id})</td>
    <td>@Html.ActionLink("Удалить пользователя","DeletePerson","Admin", new{Id = User.Id})</td>
        </tr>
    }
    </table>*@

    <div class="row">
@foreach(var User in Model){

        <div class="col-sm-4">
       <div class="card mb-3">
            <div class="card-header">
                <div class="text-center">
                Ф.И.О : @User.Surname, @User.Email, @User.Patronymic
                </div>
            </div>
            <div class="card-body">
                <h5 class="card-title">
            @if(User.Photo != null)
            {
                <img style="width:311px;height:311px;" src="data:image/jpeg;base64,@(Convert.ToBase64String(User.Photo))"/>
            }
            else
            {
                <img style="width:311px;height:311px;" src="/img/blank-profile-picture-973460__340.jpg"/>
            }
            </h5>
            </div>
            <div class="card-footer">
                <div class="btn btn-primary">@Html.ActionLink("Редактировать пользователя","EditPerson","Admin",
                new{Id = User.Id}, new{@style="color:white;text-decoration: none;"})</div>
                <div class="btn btn-danger">@Html.ActionLink("Удалить пользователя","DeletePerson","Admin",
                new{Id = User.Id}, new{@style="color:white;text-decoration: none;"})</div>
            </div>
        </div>
        </div>
}
</div>